# Dependency Graph

## Circular Dependencies
No circular dependencies were found by `madge`.

## Unused Packages (depcheck)
- `redis`

## Missing Dependencies
- `mongodb` (referenced in `test-mongo.js`)

## Unused Exports (ts-prune)
(Could not run `ts-prune` at root because `tsconfig.json` is missing at the root directory. Will run per-workspace in Phase 2 if needed.)
